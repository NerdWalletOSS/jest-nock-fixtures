const { expect } = global;

describe.skip('implementation', () => {
  it('1 is 1', async () => {
    expect(1).toBe(1);
  });
  it('true is true', async () => {
    expect(true).toBe(true);
  });
});
